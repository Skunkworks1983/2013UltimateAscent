#ifndef __TIME_H
#define __TIME_H

/**
 * @brief Gets the current time in milliseconds.
 *
 * This follows no particular standard, so it can't be used to get the actual time, just relative time.
 * @return the time in milliseconds
 */
double getCurrentMillis();

#endif
