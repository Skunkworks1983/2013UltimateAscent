#include "Math.h"

float fmin(float a, float b) {
	return a < b ? a : b;
}
int min(int a, int b) {
	return a < b ? a : b;
}
float fmax(float a, float b) {
	return a > b ? a : b;
}
int max(int a, int b) {
	return a > b ? a : b;
}
